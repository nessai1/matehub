import type { ChatClientOptions } from "./types";

/**
 * MateHub Chat SDK client.
 *
 * Handles WebSocket connection to chat service,
 * message sending/receiving, and channel management.
 */
export class ChatClient {
  constructor(_options: ChatClientOptions) {
    // TODO: implement
  }
}
