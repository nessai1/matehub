apiVersion: apps/v1
kind: Deployment
metadata:
  name: matehub-chat
  labels:
    app: matehub-chat
    {{- include "matehub.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.chat.replicaCount }}
  selector:
    matchLabels:
      app: matehub-chat
  template:
    metadata:
      labels:
        app: matehub-chat
    spec:
      containers:
        - name: chat
          image: {{ include "matehub.image" (dict "global" .Values.global "image" .Values.chat.image) }}
          ports:
            - name: http
              containerPort: {{ .Values.chat.service.port }}
              protocol: TCP
          env:
            {{- range $key, $value := .Values.chat.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
          resources:
            {{- toYaml .Values.chat.resources | nindent 12 }}
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: matehub-chat
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.chat.service.port }}
      targetPort: http
  selector:
    app: matehub-chat
