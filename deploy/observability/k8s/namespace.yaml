apiVersion: v1
kind: Namespace
metadata:
  name: observability
  labels:
    # Toggle: set to "matehub" to scope in-cluster DNS queries. No functional
    # effect here, just groups things visually in `kubectl get ns -l`.
    app.kubernetes.io/part-of: matehub
